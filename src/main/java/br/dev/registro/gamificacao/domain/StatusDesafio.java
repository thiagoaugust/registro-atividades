package br.dev.registro.gamificacao.domain;

/** Desfecho de uma instancia. Perdido e so o que venceu sem ser cumprido — nao tira nada. */
public enum StatusDesafio {
    ABERTO,
    CUMPRIDO,
    PERDIDO
}
